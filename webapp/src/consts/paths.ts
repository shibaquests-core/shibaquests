export const PATHS = {
  // PATHS - START
  index: '/',
  app: '/app',
  configureContract: '/configure-contract',
  deploy: '/deploy',
  questsCollectionView: '/quests-collections/:id',
  // PATHS - END
} as const;
